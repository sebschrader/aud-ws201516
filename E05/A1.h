// This header files declares objects, that are defined elsewhere

// Declare the sequence function
long long sequence(int);

// Declare the sequence_name variable, the extern keyword makes this a variable
// declaration instead of a definition
extern const char *sequence_name;

